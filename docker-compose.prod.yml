version: '3.8'

services:
  react:
    build:
      context: .
      dockerfile: react.Dockerfile
      target: prod
    container_name: react
    expose:
      - '8081'
    ports:
      - '8081:8081'