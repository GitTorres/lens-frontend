version: '3.8'

services:
  ts-react-app-prod:
    build:
      dockerfile: app.Dockerfile
      target: production
    container_name: ts-react-app-prod
    expose:
      - '80'
    ports:
      - '8080:80'
